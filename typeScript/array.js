"use strict";
// typescript array declaration
let nums = [1, 2, 3, 4, 5,]; //same to all data types
var arr_names = new Array(4);
// for(var i = 0;i<arr_names.length;i++) { 
//    arr_names[i] = i * 2 
//    console.log(arr_names[i]) 
// }
